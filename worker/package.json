{
  "name": "azera-mail-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for receiving emails and serving a REST API for Azera Trash Mail",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "postal-mime": "^2.2.0"
  },
  "devDependencies": {
    "wrangler": "^3.60.0"
  }
}
